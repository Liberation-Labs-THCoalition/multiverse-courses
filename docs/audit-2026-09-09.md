# Adversarial audit, 2026-09-09 — consolidated findings

**Status: `ACTIVE`.** Five Fable agents, five viewpoints, run overnight at Thomas's suggestion:
**new student · stranger facilitator · curriculum professor · returning student · hostile sceptic.**
Each read the repo cold, in character, instructed to find defects rather than agree.

---

## How much to trust this

I spot-checked the first audit's checkable claims before acting on anything: **five for five held**
(the spoiler link, a five-row table described as three, the kill count, a dangling fragment, the
unlabelled Brodeur numbers). Findings below are marked with **how many independent auditors hit
them**, because convergence across personas that never saw each other's work is the strongest
signal available here.

**Where a finding is architectural rather than a defect, I have not fixed it.** Those are scope
decisions, and they are marked 🔴.

---

## 🔴 THE DECISION THAT BLOCKS THE OTHERS — *arc vs. rolling* — **3 of 4 auditors**

The repo contains **two incompatible course designs, both live, neither marked as superseded.**

| the arc model | the rolling model |
|---|---|
| session files (2026-08-25) | target bank + running-a-session (2026-09-08/09) |
| *"Walks in with… one earned kill from session 1"* | *"each is self-contained by design"* |
| kills accumulate 1→2→3 across sessions | students join at any session |
| capstone requires a repo built over 60 days | monthly, rolling body |
| `OUTLINE_v2.md:3` — *"supersedes the arc… stands until accepted"* | no acceptance record exists |

**This is mine.** I wrote the target bank and the timing doc asserting a rolling model without
reconciling them against four session files that implement a cohort arc. The target bank fixed
*target* burn and I then claimed it had fixed *state* burn, which it had not.

**Consequences that all resolve once you pick:** a session-3 joiner has no runnable exercise in any
of that session's four hours; a session-4 joiner has zero kills for hour 1; the seeded-01 collision
below; and *"where do the 60 days start"* is unanswerable under rolling.

**Pick one. I can implement either.**
- **Arc** → drop the rolling claim, restore a closed cohort, and the target bank shrinks to
  "one target per session within a cohort."
- **Rolling** → every session needs an in-room way to *generate* the state it consumes (a supplied
  starter kill, a supplied prereg), and the walks-in/walks-out headers all get rewritten.

---

## 🔴 SEVERITY 1 — Invented student observations — **professor**, and unambiguous

**The course has never been taught. There are no students.** Yet facilitator notes asserted student
behaviour in the present tense as observed fact — *"students consistently under-estimate
saturation," "most rooms will," "these recur," "everyone says yes."*

That is Greenberg's **invention** category: the thing session 3 now teaches, committed in the same
repo, on the same day, by me.

**FIXED overnight.** Nine instances marked **`PREDICTED`**, with a convention added to
`CONTRIBUTING.md`: any claim about what students actually do is either `PREDICTED` or
`OBSERVED <date>, n=<cohorts>`, and **the unmarked present tense is the failure mode.**

The repair is better than deletion would have been: there is now a dated, falsifiable record of
what we expected before we had evidence, and **cohort one is the test.** That is the course's own
method, applied to the course.

---

## 🔴 SEVERITY 1 — Session 1's two central hours have no materials — **2 of 4**

- **Hour 2's prediction game**: "three surprising claims about model behaviour" — the three claims
  and their answers **do not exist in the repo.**
- **Hour 3's tracing exercise**, the self-described centrepiece: "each student receives a short
  claim and a pointer to the artifact behind it," seeded with five failure modes — **the claim set
  does not exist**, and `session-1` still carries it as `OPEN`.

A stranger cannot author these: they are drawn from our papers. **Session 1 is currently
undeliverable by anyone but us**, which fails the handoff test the course itself teaches.

---

## SEVERITY 2 — The repo is its own spoiler channel — **3 of 4**

Answer keys sit in student-reachable files in a public repo:

- `courses/vibe-research/README.md` printed **seeded-01's complete answer** — **FIXED overnight.**
- `the-target-bank.md` prints every target's reveal, and `session-1` links students straight to it.
  **NOT FIXED** — needs a spoiler-free student handout, which is real work.
- `docs/between-sessions-and-tone.md` prints four kill entries plus seeded-01's defect, directly
  contradicting *"do not supply our kill list, not in any session."* **NOT FIXED.**
- `FACILITATOR.md` files sit beside the student files. "Do not hand this out" is honour-system in a
  cloneable repo. **Needs a distribution mechanism**, not a stronger warning.

### 🔴 And the collision, which needs your decision

**Session 2 hour 3 teaches seeded-01's answer for a full hour** — same orbit-of-4, same 0.92
confidence, same *"principled"*, same fix — and **session 4 hour 2 then runs seeded-01 as a
discovery exercise.** Verified directly, not taken on the auditor's word.

Session 4's run-sheet is written for a naive room that our own sequence guarantees will not exist.
Flagged in `session-2` with three ways out; **not resolved, because it is a design call.**

---

## SEVERITY 2 — Assessment is unscheduled, unrubricized, and gameable — **2 of 4**

- The course-defining assessment — the gated vs. ungated paired run — **appears in no session
  clock.** Session 4's minute-by-minute has zero minutes for it.
- No rubric exists for session 4's graded point, and the file itself says the criterion *"belongs
  in the rubric, not just the prose."* There is no rubric to put it in.
- **Gameable:** the student writes both the analysis and the gate and knows the gate's contents, so
  the rational move is to seed a defect the gate already catches. The professor notes we supply the
  argument for why a grader won't catch this — our own founding claim that fabrication is
  imperceptible at reading speed.
- Standards: session headers cite standards **that do not exist in the index**; listed standards
  have no assessing session; eight of thirteen are stubs; no performance levels exist despite the
  XP-integration claim.

---

## SEVERITY 3 — The novice cannot follow it — **new student**

`AUROC`, `FWL`, `residualisation`, `permutation null`, `p-value`, `α`, `estimator`, `power`,
`confidence interval` — **all used, none defined**, most in session 2 hour 1.

And the structural one behind it: **no stage teaches you to choose or construct a statistical
test.** The pipeline goes Design → Pre-register → Execute → Analyse; "pick your test" appears
nowhere. Session 2 hour 3 says *"every student runs their planned test on pure noise"* — assuming a
planned test the course never taught them to build.

Stated drop-out point: session 2 hour 4, asked to write a pre-registration naming an estimator,
sensitivity analyses, and power — four undefined terms in one deliverable.

---

## SEVERITY 3 — Clocks — **3 of 4**

Sessions 1–3 have **no clock at all** and were written against 60-minute blocks that do not exist.
The facilitator's attempted session-1 run sheet puts **hour 4 at ~70 minutes of tasks in a
50-minute slot**. Session 4's clock allocates all 240 minutes — **zero transitions, zero buffer**,
against our own arithmetic that budgets 19 minutes for both.

Also: hour 1's pair exercise needs pre-posted pairs, but the rule says post them *at the break
before*, and there is no break before hour 1. **Pairs must be posted pre-session; no file says so.**

---

## Fixed overnight (verified)

| | |
|---|---|
| 9 invented observations → `PREDICTED`, convention added | ✅ |
| seeded-01's answer removed from student-facing README | ✅ |
| Duplicate instructor note (verbatim, twice) | ✅ |
| "three failure modes" over a five-row table | ✅ |
| Kill count 57 / 59 / 54 across three files → generalised, no count | ✅ |
| Dangling fragment mid-file in session 2 | ✅ |
| Brodeur's 94/91/37 given arm labels | ✅ |
| seeded-01 collision flagged in-place with three options | ✅ |

---

## Still open, ranked

1. 🔴 **Arc vs. rolling.** Blocks 4 other findings. Yours.
2. 🔴 **seeded-01 collision.** Three options written. Yours.
3. **Session 1 hours 2–3 materials.** Needs our papers; ours to author.
4. **Spoiler-free target handout** + a student-file distribution mechanism.
5. **Glossary**, and a decision about where "choose a test" lives.
6. **Assessment**: schedule the paired run, write the rubric, add an anti-gaming provision — the
   professor's suggestion is good: *the facilitator supplies the seeded defect, not the student.*
7. **Clock pass on sessions 1–3**, and pre-session pairing added to the timing doc.
8. **Two Brodeur overreaches** the professor flagged — including one of mine from tonight, where I
   fastened *"twenty minutes of swapping closes more of it than a checklist ever has"* to a study
   that measured neither swaps nor checklists.

---

## The honest summary

**The exercises pass the handoff test. The sessions fail it.** `seeded-01`, `seeded-02` and
`build-grim` are runnable cold by a stranger. Sessions 1–3 are well-argued design documents wearing
lesson plans' clothes — which is precisely the defect session 4 hour 3 teaches: **a right shape
fastened to the wrong claim.**

Tonight's work was polish on a structure with two foundations. That was worth finding before a room
full of people found it instead.
